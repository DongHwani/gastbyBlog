---
title: Reflection과 Annotation
date: 2020-10-17
tags:
  - Java
  - Reflection
  - Annotation 
---  

 Java의 리플렉션에 대한 간단한 설명과 사용법, 그리고 어노테이션과 리플렉션을 사용한 예제에 대해 정리하였다. 

### 1. Reflection 

 리플렉션은 클래스 내부 정보인 클래스명, 메서드명, 필드명 혹은 값 등을 들여다 볼 수 있을 뿐만 아니라 생성 및 실행을 할 수 있는 API이다. 일종의 클래스 내부의
 context한 모든 정보를 확인 및 조작, 실행할 수 있다. 

~~~Java
public class ReflectionTest {

    @Test
    public void reflection_classInformation() throws Exception{

        Class<?> clazz = Class.forName("sports.Player");
        Constructor<?> constructor = clazz.getConstructor(int.class);
        Player son = (Player) constructor.newInstance(11);

        assertEquals("sports.Player", clazz.getName());
        assertNotNull(son);
        assertEquals(11, son.getNumber());

        //reflections 라이브러리
        Reflections reflections = new Reflections("sports");
        Set<Class<?>> classes = reflections.getSubTypesOf(Object.class);

    }
}
~~~
기본적으로 제공하는 java api에서는 리플렉션을 통해서 단 하나의 클래스만 접근할 수 있는 걸로 알고 있다. 여기서 특정 패키지 이하의
모든 클래스들에 대한 정보를 가져오고 싶을 경우 reflections라는 라이브러리를 사용해서 여러 클래스 정보들을 가져올 수 있다.

~~~Java



~~~

### 2. Annotation 
어노테이션은 일종의 라벨지처럼 클래스, 필드, 생성자, 메서드에 다양하게 붙어서 주석처럼 마크하는 역할과 특정 기능들을
수행하게 하는 API이다.



### Reflection && Annotation 
 어노테이션


 
[Reference]

